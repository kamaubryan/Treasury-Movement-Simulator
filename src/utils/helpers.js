// This file can be used for various helper functions, e.g., formatting, validation.
// For now, uuid is directly imported where needed.

// export const formatCurrency = (amount, currency) => {
//     return new Intl.NumberFormat('en-US', {
//         style: 'currency',
//         currency: currency,
//         minimumFractionDigits: 2,
//         maximumFractionDigits: 2,
//     }).format(amount);
// };